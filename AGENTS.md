# AGENTS.md

## 项目目的

这是一个独立、本地可运行的「电子料 BOM 报价审核台」售前 Demo。目标是证明流程与交付形态，不证明真实客户效果。

## 不可突破的边界

- 只使用 `fixtures/` 中明确标注为“模拟数据”的虚构测试集。
- 主路径必须通过本地 Node 服务真实调用 OMA `runTasks()` 和配置的模型 API；API Key 只允许存在于服务端进程环境中。
- 备用路径必须明确标记为“离线回放”，不得静默降级，不得把回放描述成当前实时模型执行。
- 不连接 ERP、邮箱、报价库、供应商系统；不联系供应商；不执行采购或下单。
- 替代料只能标记为待工程审核，不得描述为已认证。
- 不虚构客户案例、节省比例、准确率、毛利提升、成交提升或上线状态。
- 实时 OMA、离线回放和确定性规则必须分别记录并展示，不能混淆证据状态。

## 技术约定

- React + TypeScript + Vite + 本地 Node 服务；业务判断保持在 `src/domain/` 的纯函数中。
- 输入格式解析集中在 `src/domain/importers.ts`，导出集中在 `src/domain/exports.ts`。
- 行为改动必须补测试；运行 `npm run verify` 完成最小验收。
- OMA 作为 npm 依赖使用，不修改 `/Users/jack/DEV/open-multi-agent`。
- 浏览器只调用本机 `/api`；除模型 provider 外，服务端不得访问任何外部系统。
- 模型只负责开放文本抽取、归类、消歧建议和证据复核；价格、税费、库存、MOQ、交期、毛利、审批和导出由确定性代码控制。
- 实时路径失败时必须返回真实失败，只有用户明确点击后才能进入离线回放。
- 保持界面为业务审核软件，不增加 Agent 头像、聊天过程、登录、多租户、支付等无关功能。

## 常用命令

```bash
npm ci
npm run dev
npm run verify
npm run reset
```
